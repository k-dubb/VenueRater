<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
</head>
<body>

<form name="search" id="search" method="" action="">
    <div class="form-group">
      <input class="form-control" type="text" name="q" id="q" value="" placeholder="Search for a venue by city"/>
    </div>
    <input class="btn2 btn-primary" id="search2" type="submit" name="submit" value="Search"/>
  </form>

<ul id="results"></ul>

<script src="https://code.jquery.com/jquery-2.2.0.min.js"></script>
<script type="text/javascript">


$(function() {

    // Search SeatGeek API for all venues
    var apiRoot = 'http://api.seatgeek.com/2/venues/';

    var loadVenues = function(city){
      
      var jqxhr = $.ajax({
        url: apiRoot,
        method: "GET",
        data: {
          // name: name,
          // address: address,
          city: city
          // state: state,
          // postal_code: postal_code,
          // id: id,
          // url: url
        },
        dataType: "jsonp",
        jsonCallback: 'info'
      })
      .done(function(data) {
        $('#results').empty();
        var venues = data;

        for (var i=0; i<data.venues.length; i++) {

          var content = '<a href="/rate/' + data.venues[i].id + ' "><li>' + data.venues[i].name + '</li></a>';
              
         console.log(content);
         $('#results').append(content);

        }
      })
      .fail(function(jqXHR, textStatus) {
        console.log("Request failed: " + textStatus);
      })
      .always(function() {
        // console.log('Request completed');
      });

      function info(venues){
        alert(venues);
      };
    };

    var displayVenues = function(venues){
      for (var i=0; i<data.venues.length; i++){
        var venue = data.venues[i];

        appendVenue();
      }
    };

    var appendVenue = function(venue){
      $('#venues').append(venue);

        console.log(venue);
    };

    // Search 
    $('#search').on('submit', function(evt) {
      evt.preventDefault();
      var searchTerm = $('#q').val();

      loadVenues(searchTerm);

      function info(venues){
        alert(venues);
      };
    
  });
});



// Show one venue's data
// $(function() {

//     var apiRoot = 'http://api.seatgeek.com/2/venues/';

//     var loadVenues = function(){
      
//       var jqxhr = $.ajax({
//         url: apiRoot + data.venues[i].id,
//         method: "GET",
//         data: {},
//         dataType: "jsonp",
//         jsonCallback: 'info'
//       })
//       .done(function(data) {
//         var venues = data;

//         for (var i=0; i<data.venues.length; i++) {

//           var content = '<li>' + data.venues[i].name  + '</li>';
              
//          console.log(content);
//          $('#faves').append(content);

//         }
//       })
//       .fail(function(jqXHR, textStatus) {
//         console.log("Request failed: " + textStatus);
//       })
//       .always(function() {
//         // console.log('Request completed');
//       });

//       function info(venues){
//         alert(venues);
//       };
//     }
// });




</script>
</body>
</html>